// No JS is currently required for nav state — each page (index.html,
// biography.html, events.html, media.html, contact.html) hardcodes the
// "active" class on its own nav link directly in the markup, since they're
// now separate pages rather than anchors within one scrolling page.
//
// This file is kept as a hook for future behavior (e.g. a mobile nav
// toggle, once .nav-links is no longer just hidden under 820px).
